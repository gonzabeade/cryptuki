import {AxiosService} from "../services/AxiosService";
import axios, {AxiosInstance} from "axios";

//TODO: mirar como testear los handlers de interceptor de axios
//describe("request interceptor", () => {
    //it("has to add Basic credentials", () => {
        //Setup
    //    let axiosServiceInstance = AxiosService
    //        .getInstance()
    //        .useBasicAuthentication("salvaCasta", "casta_salva")

        //Ejecicion
        //AxiosService.getAxiosInstance().interceptors.request.handlers[0];

        //Assertions

    //})
//});

describe("response interceptor", () => {
    it("", () => {})
})